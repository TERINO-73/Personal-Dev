package com.jesusterino.app.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // CORS configuration is now handled entirely by Spring Security in
    // SecurityConfig.java
}
